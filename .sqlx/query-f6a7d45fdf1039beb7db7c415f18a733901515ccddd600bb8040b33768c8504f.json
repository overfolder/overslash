{
  "db_name": "PostgreSQL",
  "query": "INSERT INTO permission_rules (org_id, identity_id, action_pattern, effect)\n         SELECT $1, i.id, pattern, 'allow'\n           FROM identities i\n          CROSS JOIN unnest($2::text[]) AS pattern\n          WHERE i.org_id = $1\n            AND i.kind = 'agent'\n            AND i.depth = 1\n            AND i.archived_at IS NULL\n            AND EXISTS (\n                SELECT 1 FROM orgs o\n                 WHERE o.id = $1 AND o.default_agent_self_setup)\n            AND NOT EXISTS (\n                SELECT 1\n                  FROM permission_rules r\n                 WHERE r.org_id = $1\n                   AND r.identity_id = i.id\n                   AND r.action_pattern = pattern)\n         RETURNING identity_id",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "identity_id",
        "type_info": "Uuid",
        "origin": {
          "Table": {
            "table": "permission_rules",
            "name": "identity_id"
          }
        }
      }
    ],
    "parameters": {
      "Left": [
        "Uuid",
        "TextArray"
      ]
    },
    "nullable": [
      false
    ]
  },
  "hash": "f6a7d45fdf1039beb7db7c415f18a733901515ccddd600bb8040b33768c8504f"
}
