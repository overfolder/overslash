{
  "db_name": "PostgreSQL",
  "query": "INSERT INTO permission_rules (org_id, identity_id, action_pattern, effect)\n         SELECT $1, $2, pattern, 'allow'\n           FROM unnest($3::text[]) AS pattern\n          WHERE EXISTS (\n                SELECT 1\n                  FROM orgs o\n                  JOIN identities i ON i.org_id = o.id\n                 WHERE o.id = $1\n                   AND i.id = $2\n                   AND o.default_agent_self_setup\n                   AND i.kind = 'agent'\n                   AND i.depth = 1\n                   AND i.archived_at IS NULL)\n            AND NOT EXISTS (\n                SELECT 1\n                  FROM permission_rules r\n                 WHERE r.org_id = $1\n                   AND r.identity_id = $2\n                   AND r.action_pattern = pattern\n                   AND (r.expires_at IS NULL OR r.expires_at > now()))",
  "describe": {
    "columns": [],
    "parameters": {
      "Left": [
        "Uuid",
        "Uuid",
        "TextArray"
      ]
    },
    "nullable": []
  },
  "hash": "7e035556d435fe656dd995035e6e55be997a322a3dd9144ccb661be76bc41729"
}
