# Problems detected 20th April

+ When the agent gets pending approval it suggest going to a not existing domain
```
● The delete call returned pending_approval — Google Calendar writes are gated, and self-approval was blocked (correctly: you, not the agent, need to authorize the delete).

  To proceed, please approve it here:

  👉 https://overslash.example/approve/e1718c94906550548431326fa8c5311ed6c9cb472c6e3b0eb1c934c47faf6cf7
```
Fix: Overslash should have an envvar with the canonical dashboard domain for this

+ Approvals on the /agents dashboard dont have inline "Allow Once"
+ Approval page is a standalone from the dashboard, i would rather prefer a modal overlay, or a dropdown with the expiry and different granularity options
+ Bubble Up should'nt be an option when the User is already the assigned approval resolver

+ On MCP login, the assignment page shouldn't be server from api, but from dashboard
+ By default MCP logins wont inherit_permissions
+ MCP login on reauthentication should reuse the existing agent
+ UI: True/False controls should be prettier Toggle Switch, add to Figma Design System and adopt everywhere(Inherits Permissions)
+ Pending Approvals UI date shows as "Requested Invalid Date", likely the serialization format is not understood by frontend
+ UI: No Audit Log view yet
+ On Services the "overslash" meta service has Credentials as "Needs Setup", this makes no sense
+ UI: No API Explorer
+ UI: Notification bell doesnt show a dropdown of notifications
+ Dont show revoked MCP Clients, on UI remove then from view after 3 secs
+ Choosing existing oauth google conns doesnt work
+ Services view shows owner as an UUID, not helpful, show Username instead? or "You"? or "user"?
+ http://127.0.0.1:3000/users/Dev%20User 404s
+ UI: Services should be connectable to groups from the service detail view, and groups that have it should appear as pills on a column in the service list
+ Templates: all should have a category field!

+ OAuth Connections: after adding a Google OAuth Client ID i see a new "Identity Provider" (which doesnt really affect login) and a new UUID Connection ID for new google service, which doesnt work

+ Future: we might have to support multi-org login on Cloud Overslash, to be designed

+ We should support reusing connection on services with the same provider OAuth
+ Connections shouldn't be UUIDs, show emails, generally the user identity on the provider
+ Make sure we support incremental scopes auth
