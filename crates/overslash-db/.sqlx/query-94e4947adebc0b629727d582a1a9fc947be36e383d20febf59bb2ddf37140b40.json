{
  "db_name": "PostgreSQL",
  "query": "WITH RECURSIVE subtree(id, lvl) AS (\n                SELECT id, 1 FROM identities WHERE parent_id = $1\n                UNION ALL\n                SELECT i.id, s.lvl + 1\n                FROM identities i\n                INNER JOIN subtree s ON i.parent_id = s.id\n                WHERE s.lvl < $4\n            )\n            UPDATE identities SET\n                depth = depth + $2,\n                owner_id = CASE WHEN kind = 'sub_agent' THEN $3 ELSE owner_id END,\n                updated_at = now()\n            WHERE id IN (SELECT id FROM subtree)",
  "describe": {
    "columns": [],
    "parameters": {
      "Left": [
        "Uuid",
        "Int4",
        "Uuid",
        "Int4"
      ]
    },
    "nullable": []
  },
  "hash": "94e4947adebc0b629727d582a1a9fc947be36e383d20febf59bb2ddf37140b40"
}
