openapi: 3.1.0
info:
  title: Test
  key: test-svc
servers:
  - url: https://api.example.com
paths:
  /items/{id}:
    get:
      operationId: get_item
      summary: "Get {id}"
      risk: read
      scope_param: missing
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
